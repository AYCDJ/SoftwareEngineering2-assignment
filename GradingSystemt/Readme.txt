The program will ask the user to choose between using randomly generated grades or to input grades manually.

Options are selected with either 1 or 2,

Regardless of choice the user will also be asked how many student grades they wish to generate.

For manual input the program will only accept values between 0 and 100 and anything else will cause the program to end. 

